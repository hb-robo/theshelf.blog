---
// src/pages/index.astro
import BaseLayout from "../layouts/BaseLayout.astro";
import ReviewCard from "../components/ReviewCard.astro";
import SpineDisplay from "../components/SpineDisplay.astro";
import { getCollection } from "astro:content";
import type { ExpandedMediaItem } from "../lib/types";
import { getExpandedMediaList } from "../lib/expandMediaItems";

const allReviews = await getCollection("reviews");
const publishedReviews = allReviews.filter((r) => r.data.published);
const recentReviews = publishedReviews
  .sort(
    (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
  )
  .slice(0, 5);

const expandedMediaList: ExpandedMediaItem[] = await getExpandedMediaList();
const publishedMedia = expandedMediaList.filter((m) => m.published);
const stripItems = publishedMedia.slice().sort((a, b) => a.title.localeCompare(b.title));

const totalReviews: number = publishedReviews.length;
const totalMedia: number = publishedMedia.length;
const totalOnShelf: number = publishedMedia.filter(
  (m) => m.result === 'shelved'
).length;
const approvalRate =
  totalMedia > 0 ? Math.round((totalOnShelf / totalMedia) * 100) : 0;
---

<BaseLayout title="theshelf.blog" bigNav>
  <!-- Hero Section -->
  <section>
    <div class="max-w-4xl mx-auto px-4 py-12 md:py-8">
      <p class="max-w-2xl text-xl md:text-2xl font-serif text-zinc-900 leading-snug">
        musings on the ultimate zero-sum game:<br />what books, albums, games, and films make my shelf?
      </p>
    </div>
  </section>

  <!-- Shelf Strip -->
  <section style="--shelf-scale: 0.8;">
    <div class="max-w-4xl mx-auto px-4 flex justify-between items-center py-3">
      <span class="text-xs font-mono text-zinc-400 uppercase tracking-widest">on the shelf lately</span>
      <a href="/shelf" class="text-xs font-mono text-zinc-400 hover:text-zinc-900 transition-colors">explore the full shelf →</a>
    </div>
    <div class="max-w-4xl mx-auto px-4">
      <div class="bg-zinc-100 border border-zinc-200 shadow-inner overflow-hidden">
        <div class="flex items-end gap-0.5 px-4 pt-4 pb-3 overflow-x-auto overflow-y-hidden">
          {stripItems.map(item => <SpineDisplay itemData={item} />)}
        </div>
      </div>
    </div>
  </section>

  <!-- Recent Reviews -->
  <section class="max-w-4xl mx-auto px-4 py-8">
    <div class="flex justify-between items-center border-b border-zinc-200 py-3 mb-6">
      <span class="text-xs font-mono text-zinc-400 uppercase tracking-widest">recent thoughts</span>
      <a href="/reviews" class="text-xs font-mono text-zinc-400 hover:text-zinc-900 transition-colors">view all →</a>
    </div>
    <div class="space-y-6">
      {recentReviews.map((review) => <ReviewCard itemData={review} />)}
    </div>
  </section>

  <!-- Stats Overview -->
  <section class="max-w-4xl mx-auto px-4 py-8">
    <div class="flex justify-between items-center border-b border-zinc-200 py-3 mb-6">
      <span class="text-xs font-mono text-zinc-400 uppercase tracking-widest">some numbers</span>
      <a href="/stats" class="text-xs font-mono text-zinc-400 hover:text-zinc-900 transition-colors">more data →</a>
    </div>
    <div class="border border-zinc-200 grid grid-cols-2 md:grid-cols-4 divide-x divide-y md:divide-y-0 divide-zinc-200 font-mono">
      <div class="p-6 text-center">
        <div class="text-4xl text-zinc-900 mb-2">{totalReviews}</div>
        <div class="text-xs text-zinc-400 uppercase tracking-widest">reviews</div>
      </div>
      <div class="p-6 text-center">
        <div class="text-4xl text-zinc-900 mb-2">{totalMedia}</div>
        <div class="text-xs text-zinc-400 uppercase tracking-widest">works covered</div>
      </div>
      <div class="p-6 text-center">
        <div class="text-4xl text-zinc-900 mb-2">{totalOnShelf}</div>
        <div class="text-xs text-zinc-400 uppercase tracking-widest">shelved</div>
      </div>
      <div class="p-6 text-center">
        <div class="text-4xl text-zinc-900 mb-2">{approvalRate}%</div>
        <div class="text-xs text-zinc-400 uppercase tracking-widest">approval</div>
      </div>
    </div>
  </section>
</BaseLayout>
